import { Context } from "./Enum/Context";
import { StatBonus } from "./StatBonus";

export class LeaderBuff{

    context : Context;
    bonus : StatBonus;
    contextIcon : string;
    bonusIcon : string;
}