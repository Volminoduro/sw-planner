export class Stat{

    name : string;
    isPercentage : boolean;

    constructor(name: string, isPercentage: boolean){
        this.name = name;
        this.isPercentage = isPercentage;
    }
}